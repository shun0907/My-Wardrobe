<% provide(:title, "Edit user") %>
<% provide(:url, user_path) %>
<% provide(:button_text, 'Save changes') %>

<div class="row background-color2">
  <h1 class="top-space">Update your profile</h1>
  <div class="col-md-6 col-md-offset-3">
      <%= render 'form' %>
    <div class="gravatar_edit">
      <%= gravatar_for @user %>
      <div class="under-space-Update">
        <a href="http://gravatar.com/emails" target="_blank" rel="noopener">change icon</a>
      </div>
    </div>
  </div>
</div>