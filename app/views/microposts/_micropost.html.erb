<li id="micropost-<%= micropost.id %>">
  <%= link_to gravatar_for(micropost.user, size: 50), micropost.user %>
  <span class="user"><%= link_to micropost.user.name, micropost.user %></span>
  <span class="content">
    <div class="feed-title">Item</div>
    <div class="feed-contents"><%= micropost.content %></div>
    <div class="feed-title">Brand</div>
    <div class="feed-contents"><%= micropost.content2 %></div>
    <div class="feed-title">Memo</div>
    <div class="feed-contents"><%= micropost.content3 %></div>
    <div class="picture-contents">
      <%= image_tag micropost.picture.url if micropost.picture? %>
    </div>
  </span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(micropost.created_at) %> ago.
    <% if current_user?(micropost.user) %>
      <%= link_to "delete", micropost, method: :delete,
                                       data: { confirm: "You sure?" } %>
    <% end %>
  </span>
</li>